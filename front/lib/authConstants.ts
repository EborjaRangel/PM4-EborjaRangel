/** Sesión cliente: solo perfil público + JWT — nunca contraseñas. */
export const AUTH_TOKEN_LS_KEY = "pulse_auth_token";
export const SESSION_PROFILE_LS_KEY = "pulse_session_profile";

/** Migración desde versiones antiguas (usuarios locales con contraseña). */
export const LEGACY_USERS_KEY = "pulse_users";
export const LEGACY_CURRENT_USER_KEY = "pulse_current_user_id";
